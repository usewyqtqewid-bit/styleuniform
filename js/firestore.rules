rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /productos/{doc} {
      allow read: if true;     // lectura pública
      allow write: if false;   // escritura solo vía Functions/Admin
    }
  }
}